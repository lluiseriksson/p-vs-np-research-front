# Cycle 164 — exact-table handoff audit

**Label: PROVED**

LEMMA-197 embeds the size-three carrier and a cross-row boundary handoff into
one explicit single-output circuit computing the exact `A,A,0,A` table. The
circuit is redundant and not a plateau.

Therefore full four-code signature consistency alone is `NO-GO`.
GATE-004CR is active and must charge a handoff through minimum cost, an extra
deletion, a private certificate, or forbidden cycle loss.

## Classification

- LEMMA-197: `PROVED`
- GATE-004CQ-FOUR-CODE-SIGNATURES-ONLY: `NO-GO`
- GATE-004CR: `EXPLORATORY`
